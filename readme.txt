put image in the same folder with source code
run the following command "python test.py -i {pathToImage} -c {N.O.ClusteringColor}"
WARMNING: source code will generate serveral images as the result of the process, so put all your working file in a folder for the risk of being spam